import "./globals.css";
import { Poppins } from "next/font/google";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["300","400","500","600","700","800","900"],
  variable: "--font-poppins",
});

export const metadata = {
  title: "Piris Katwe Inc — We Build Brands That Dominate",
  description: "Strategic brand building, paid advertising, content creation, motion design, and influencer marketing that scales your business.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={poppins.variable}>
      <body>{children}</body>
    </html>
  );
}
